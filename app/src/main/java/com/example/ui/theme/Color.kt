package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Clean Minimalism Base Palette
val MidnightBg = Color(0xFF111318)       // Deep slate-dark background
val MidnightSurface = Color(0xFF1E1F23)  // Bottom nav and app bars background
val MidnightCard = Color(0xFF2E3033)     // Rounded primary card background

// High-fidelity Lavender/Purple Accent layers (matching design HTML)
val MintAccent = Color(0xFFD0BCFF)       // Primary lavender accent
val MintSecondary = Color(0xFFEADDFF)    // Complementary muted lavender
val MintLight = Color(0xFFF3EDFF)        // Extremely soft companion color

// Functional clean feedback colors
val ErrorRed = Color(0xFFF87171)         // Clean warm red
val TextPrimary = Color(0xFFE2E2E6)      // Clean reader active text
val TextSecondary = Color(0xFF919194)    // Muted secondary caption text
val BorderDark = Color(0x4D44474E)       // #44474E with 30% opacity matching custom borders
